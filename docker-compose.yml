services:
  benchmark-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - COMPUTE_ITERATIONS=100000
    cpus: 1.0
    mem_limit: 512m
    mem_reservation: 256m
    restart: unless-stopped
